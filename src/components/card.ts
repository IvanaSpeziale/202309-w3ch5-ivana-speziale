/* Import { Component } from './component';
import { Pokemon } from '../model/type'; */
/* Import '../scss/style.scss'; */
/* 
export class Card extends Component {
  pokemon: Pokemon;
  template: string;

  constructor(selector: string, pokemon: Pokemon) {
    super(selector);
    this.pokemon = { ...pokemon }; // Asegurarse de copiar el objeto Pokemon
    this.template = this.createTemplate();
    this.render();
  }

  render() {
    super.render();
  }

  createTemplate() {
    return `
    <li class="pokemon-card">
      <img src="${this.pokemon.imgUrl}" alt="${this.pokemon.name}">
      <p><span>Name:</span> ${this.pokemon.name} </p>
      <p><span>URL:</span> ${this.pokemon.url} </p>
    </li>`;
  }
} */
